import { NextPage } from "next";
import { ReactElement } from "react";
import TopPageWrapper from "../components/TopPageWrapper";
const index: NextPage<any> = (): ReactElement => {
    return <TopPageWrapper/>
}

export default index;